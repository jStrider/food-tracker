# Backend API URL for Docker environment
# Frontend runs in the browser, so it needs to connect to the exposed host port
# Backend is exposed on port 3002 in docker-compose.yml (3002:3001)
VITE_API_URL=http://localhost:3002